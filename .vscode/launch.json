{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(debug) Launch",
            "type": "cppdbg",
            "request": "launch",
            // Please change to the executable of your current lab or assignment
            "program": "${workspaceFolder}/bin/ass2",
            "args": ["Assignment-2/testcase/src/test1.c"], // may input the test llvm bc file or other options and flags the program may use
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "C/C++: cpp build active file",
            "MIMode": "gdb",
            "setupCommands": [
                {
                    "description": "Enable pretty-printing for gdb",
                    "text": "-enable-pretty-printing",
                    "ignoreFailures": true
                }
            ] 
        }
    ]
}
